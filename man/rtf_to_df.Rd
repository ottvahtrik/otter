% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rtf_to_df.R
\name{rtf_to_df}
\alias{rtf_to_df}
\title{Parse an RTF file and extract table body cells into a data frame}
\usage{
rtf_to_df(
  path,
  include_header = FALSE,
  header_row = NULL,
  encoding = "UTF-8",
  table_index = NULL
)
}
\arguments{
\item{path}{Character scalar. Path to the \code{.rtf} file.}

\item{include_header}{Logical scalar. If \code{TRUE}, column names
are taken from the detected header row. When no header row is auto-detected,
the first body row is used as column names and dropped from the data. \strong{Default:} \code{FALSE}}

\item{header_row}{Integer scalar. Which header row to use as
column names when \code{include_header = TRUE} and there are multiple detected header
rows. Defaults to the last header row. Use \code{1L} for the first header row. \strong{Default:} \code{NULL}}

\item{encoding}{Character scalar. Encoding passed to
\code{\link[=readLines]{readLines()}} when reading the RTF file. Use \code{"latin1"} for older SAS RTF
that is not UTF-8. \strong{Default:} \code{"UTF-8"}}

\item{table_index}{Integer scalar. Index of the table to
extract when the RTF contains multiple distinct tables. \code{1L} selects the
first table, \code{2L} the second, etc. \code{NULL} merges all tables. \strong{Default:} \code{NULL}}
}
\value{
A \code{data.frame} with one row per body row and one column per cell. All
values are character strings. Column names are \code{V1}, \code{V2}, ... unless
\code{include_header = TRUE}.
}
\description{
Reads an RTF file and returns a \code{data.frame} of the body cell values.
}
\details{
Header rows are detected automatically via \verb{\\trhdr} (explicit; Word/SAS) or via
cell vertical-alignment hints (\verb{\\clvertalb} / \verb{\\clvertalt}) used by \href{https://merck.github.io/r2rtf/}{\{r2rtf\}}.

When \code{include_header = TRUE} and no header rows are auto-detected, the first
body row is promoted to column names and removed from the data (analogous to
\code{read.csv(header = TRUE)}).
Footnote and source rows are excluded by their cell count (they span the full
table width as a single cell).

When an RTF file contains multiple logically distinct tables (separated by
paragraph text), use \code{table_index} to select a specific one. The default
(\code{NULL}) merges all tables into a single data frame, which is the correct
behaviour for paginated single-table outputs.
}
\examples{
path <- system.file("extdata", "example.rtf", package = "otter")
rtf_to_df(path)
rtf_to_df(path, include_header = TRUE)

multi_path <- system.file("extdata", "multi_table_example.rtf", package = "otter")
rtf_to_df(multi_path, include_header = TRUE, table_index = 2L)

}
